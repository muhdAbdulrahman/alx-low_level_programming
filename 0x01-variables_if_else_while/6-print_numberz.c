#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (Success)
 */
int main(void)
{
	int first = 0, last = 9;

	while (first <= last)
	{
		/**
		 *this code convert first to char and
		 *convert to number printable with putchar
		 */
		putchar((char)first + '0');
		start++;
	}
	putchar('\n');
	return (0);
}
