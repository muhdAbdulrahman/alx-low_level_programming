#include "main.h"

/**
* _islower - checks if the characters are in lower case
* character; the character to be checked
*
* Return: 1 character is lowercase, 0 otherwise
*/
int _islower(int character)
{
if (character >= 'a' && character <= z')
return (1);
else
return (0);
}
